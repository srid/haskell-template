name: "CI"

on:
  push:
    branches:
      - "*"
  pull_request:

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Build Nix
        run: |
          nixci
