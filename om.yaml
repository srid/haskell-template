# Documentation: https://omnix.page/config

health:
  default:
    # Warn the user if homebrew is installed <https://omnix.page/om/health>
    homebrew:
      enable: true

develop:
  default:
    readme: |
      🎯 Welcome to the **haskell-template** project

      This template provides a modern Haskell development environment.

      To run the application,

      ```sh-session
      just run
      ```

      🚀 Run 'just' to see more commands. See <https://srid.ca/haskell-template/start> for full setup guide.
